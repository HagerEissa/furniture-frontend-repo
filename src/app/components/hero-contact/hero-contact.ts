import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-hero-contact',
  imports: [RouterModule],
  templateUrl: './hero-contact.html',
  styleUrl: './hero-contact.css',
})
export class HeroContact {}
