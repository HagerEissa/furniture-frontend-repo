import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-hero-checkout',
  standalone: true,
  imports: [RouterModule],
  templateUrl: './hero-checkout.html',
  styleUrl: './hero-checkout.css',
})
export class HeroCheckout {}
