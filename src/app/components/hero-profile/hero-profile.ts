import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-hero-profile',
  imports: [RouterModule],
  templateUrl: './hero-profile.html',
  styleUrl: './hero-profile.css',
})
export class HeroProfile {}
